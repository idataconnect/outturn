apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The local cluster, plus document extraction. Selected by `skaffold dev -p tika`.
resources:
  - ../local

components:
  - ../../components/tika
